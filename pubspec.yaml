name: premiertraslados_appchofer_nuevo
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.31.0
  firebase_crashlytics: ^3.5.0
  firebase_auth: ^4.19.5
  cloud_firestore: ^4.17.3
  firebase_messaging: ^14.9.2 # <-- LÍNEA AÑADIDA
  location: ^6.0.2
  flutter_local_notifications: ^17.1.2
  permission_handler: ^11.3.1
  url_launcher: ^6.2.6
  cloud_functions: ^4.7.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/