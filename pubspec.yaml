name: premiertraslados_appchofer_nuevo
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # ==========================================================
  # <<< DEPENDENCIAS COPIADAS DE TU PROYECTO VIEJO >>>
  # ==========================================================
  firebase_core: ^4.1.0
  firebase_auth: ^6.0.2
  cloud_firestore: ^6.0.1
  url_launcher: ^6.3.1
  google_maps_flutter: ^2.7.0
  cloud_functions: ^6.0.1
  location: ^5.0.3
  # ==========================================================
  
  # Se mantiene la versión más nueva de cupertino_icons
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Se mantiene la versión más nueva de flutter_lints
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg