name: premiertraslados_appchofer_nuevo
description: "Aplicación para choferes de Premier Traslados."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # --- PAQUETES DE FIREBASE Y OTROS SERVICIOS ---
  firebase_core: ^4.1.0
  firebase_auth: ^6.0.2
  cloud_firestore: ^6.0.1
  url_launcher: ^6.2.5
  google_maps_flutter: ^2.5.3
  cloud_functions: ^6.0.1
  location: ^8.0.1
  flutter_local_notifications: ^19.4.1 

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
